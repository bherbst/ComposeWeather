package com.example.androiddevchallenge.settings

/**
 * We only support a very limited number of locations
 */
enum class Location(
  val displayText: String
) {
  Minneapolis("Minneapolis, MN"),
  MountainView("Mountain View, CA")
}